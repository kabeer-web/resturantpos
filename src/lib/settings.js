// Single-restaurant config. Edit these to match your actual rates —
// swap for a Supabase `settings` row later if you ever need these
// editable from the UI without a redeploy.
export const TAX_RATE = 0;          // e.g. 0.16 for 16% GST — 0 if not registered
export const SERVICE_CHARGE_RATE = 0; // e.g. 0.05 for a 5% service charge
export const CURRENCY = 'Rs.';
